#include "KeyboardInput.h"

// TODO does this need to run in thread ???

std::vector<Command> KeyboardInput::get_list_of_commands()
{
    std::vector<Command> return_vector;
    return return_vector;
    // TODO read the currently pressed keys and return a list of Command enums that correspond to the keyboard mapping
}